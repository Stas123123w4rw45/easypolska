# Інструкція: Як зберегти прогрес на Railway

## Проблема
SQLite база даних зберігається у файловій системі контейнера, яка **стирається при кожному редеплої**.

## Рішення: PostgreSQL на Railway

### Крок 1: Додай PostgreSQL до проекту

1. Відкрий свій проект на Railway
2. Натисни **"+ New"** → **"Database"** → **"Add PostgreSQL"**
3. Railway автоматично створить базу даних і додасть змінну `DATABASE_URL`

### Крок 2: Перевір змінні оточення

Railway автоматично додасть змінну `DATABASE_URL` у форматі:
```
postgresql://user:password@host:port/dbname
```

**ВАЖЛИВО:** Потрібно змінити на формат для async:
```
postgresql+asyncpg://user:password@host:port/dbname
```

### Крок 3: Оновити DATABASE_URL

**Варіант А (Автоматично):**
Railway сам встановлює `DATABASE_URL`, але треба додати `+asyncpg`:

1. Йди в **Variables** (змінні оточення)
2. Знайди змінну `DATABASE_URL` 
3. Зміни початок з `postgresql://` на `postgresql+asyncpg://`

**Варіант Б (Вручну):**
Скопіюй значення DATABASE_URL з PostgreSQL сервісу і додай `+asyncpg` після `postgresql`.

### Крок 4: Видали стару змінну (якщо є)

Якщо у тебе є змінна `DATABASE_URL=sqlite+aiosqlite:///data/database.db` - видали її.

### Готово! 🎉

Після наступного деплою:
- ✅ Бот використовуватиме PostgreSQL
- ✅ Твій прогрес зберігатиметься між редеплоями
- ✅ Серія днів не скидатиметься
- ✅ Всі вивчені слова залишаться

## Перевірка

Подивись у логах Railway - має бути:
```
🗄️ Database: postgresql+asyncpg://...
✅ Database initialized successfully
```

## Troubleshooting

**Помилка "asyncpg not found"**
- Перевір що `requirements.txt` містить `asyncpg==0.30.0`
- Передеплой проект

**Помилка підключення**
- Перевір що `DATABASE_URL` містить `+asyncpg`
- Перевір що PostgreSQL сервіс запущений на Railway
